abstract final class AppConstants {
  static const maxUploadBytes = 100 * 1024 * 1024;
  static const searchDebounceMs = 400;
  static const allowedExtensions = ['pdf', 'epub'];
  static const booksPerShelfRow = 3;
  static const bookCoverHeight = 190.0;
}
